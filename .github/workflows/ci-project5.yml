name: CI - project 5

on:
  push:
    paths: ['project 5/**']
  pull_request:
    paths: ['project 5/**']
  workflow_dispatch: {}

jobs:
  # TODO: Add build and test jobs
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI for project 5 - to be implemented"
