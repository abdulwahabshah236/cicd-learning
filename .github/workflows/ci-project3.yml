name: CI - project 3

on:
  push:
    paths: ['project 3/**']
  pull_request:
    paths: ['project 3/**']
  workflow_dispatch: {}

jobs:
  # TODO: Add build and test jobs
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI for project 3 - to be implemented"
