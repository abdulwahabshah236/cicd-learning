name: CI - project 10

on:
  push:
    paths: ['project 10/**']
  pull_request:
    paths: ['project 10/**']
  workflow_dispatch: {}

jobs:
  # TODO: Add build and test jobs
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI for project 10 - to be implemented"
