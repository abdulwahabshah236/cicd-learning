{
  "name": "project-1-basic-webapp",
  "version": "1.0.0",
  "description": "Simple Node.js web app for CI/CD to Azure App Service",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --coverage",
    "build": "echo \"No build step required for this simple app\"",
    "lint": "echo \"No linter configured\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}